query BoostQuery($id: String!) {
  boost(
    id: $id
  ) {
    guard
    poolSize
    token {
        decimals
    }
    strategy {
      name
      params {
        version
        proposal
        eligibility {
            type
            choice
        }
        distribution {
            type
        }
      }
    } 
  }
}